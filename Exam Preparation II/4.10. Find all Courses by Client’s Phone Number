CREATE OR REPLACE FUNCTION fn_courses_by_client(
	phone_num VARCHAR(20)
) RETURNS INT AS

$$
BEGIN
	SELECT
		COUNT(*)
	FROM courses AS c JOIN clients AS cl ON c.client_id = cl.id
	WHERE cl.phone_number = phone-num;
END;
$$
LANGUAGE plpgsql;
